services:
  memefinder:
    build: .
    restart: unless-stopped
    environment:
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN}
      BIRDEYE_API_KEY: ${BIRDEYE_API_KEY}
      BIRDEYE_API_BASE_URL: ${BIRDEYE_API_BASE_URL:-https://public-api.birdeye.so}
